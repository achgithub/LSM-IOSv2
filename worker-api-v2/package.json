{
  "name": "lsm-api-v2",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone KV-backed successor to worker-api's PWA submission queue + manager lifecycle. Pure KV, no D1 — verifies JWTs worker-api issues (never mints its own), same trust pattern the sports-data shards use. Not deployed anywhere yet; isolated from worker-api at runtime.",
  "type": "module",
  "packageManager": "pnpm@11.2.2",
  "volta": {
    "node": "22.22.3",
    "pnpm": "11.2.2"
  },
  "scripts": {
    "dev": "wrangler dev --env uk",
    "deploy:uk": "wrangler deploy --env uk",
    "types": "wrangler types --env uk",
    "typecheck": "tsc --noEmit",
    "test:smoke": "bash test/smoke.sh"
  },
  "dependencies": {
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "wrangler": "^4.100.0"
  }
}
